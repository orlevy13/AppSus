import SideBar from '../cmps/SideBar.jsx';

export default class Compose extends React.Component {

    render() {
        return (
            <section>
                <SideBar></SideBar>
                <p>Compose</p>
            </section>
        )
    }
}